---
title: "## `pipeio_run` resolves `snakemake` from a shared conda env instead of the proj"
date: 2026-04-23
timestamp: 20260423-163539-959588
status: done
result_note: /storage2/arash/worklog/workflow/captures/20260423-163836-78e529/note.md
completed: 2026-04-23T16:38:38+02:00
goal: pipeio-pixecog
actionable: true
prompt: ""
source_note: /storage2/arash/projects/projio/packages/pipeio/docs/log/issue/issue-arash-20260423-163526-145325.md
project_primary: pipeio
tags: [task]
source: worklog-promote
---

# ## `pipeio_run` resolves `snakemake` from a shared conda env instead of the proj

## Goal

(promoted from note)

## Context

(see source note)

## Prompt

> Fix the issue described in /storage2/arash/projects/projio/packages/pipeio/docs/log/issue/issue-arash-20260423-163526-145325.md. Read it first, then implement the proposed fix.

## Acceptance Criteria

- [ ] 

## Result

Completed in 148.9s (1 steps, model: opus)

Session: `3ddb09af-141c-416b-9b1a-9b5ac2f90deb`
Result note: `/storage2/arash/worklog/workflow/captures/20260423-163836-78e529/note.md`

### Output

```
The file I need to edit lives in the parent projio repo (`/storage2/arash/projects/projio/src/projio/mcp/pipeio.py`), outside my current pipeio package working dir, and permission to write there wasn't pre-approved.

The fix is in `projio/src/projio/mcp/pipeio.py` — the `_resolve_snakemake_cmd` function currently falls through to `shutil.which("snakemake")` when `code.envs.default` is unset, which in a pixi-managed project running under the MCP server's own conda env (rag) picks up the wrong snakemake binary. The fix: after step 3 (`code.envs.default`), short-circuit with `_pixi_run_cmd("snakemake")` for pixi projects so they never touch PATH.

Proposed diff for `src/projio/mcp/pipeio.py` `_resolve_snakemake_cmd` (lines 174-235):

```diff
@@ -174,8 +174,7 @@
 def _resolve_snakemake_cmd(use_env: str = "") -> list[str]:
     """Resolve the snakemake command with runner wrapping if needed.

     Checks (in order):
     1. Explicit ``use_env`` env name (routed through detected runner)
     2. ``SNAKEMAKE`` Makefile variable
-    2.5. Project config ``code.envs.default`` (avoids picking up the MCP
-         server's own conda env from PATH)
-    3. ``snakemake`` on PATH (with conda wrapping if in a conda env)
-    4. Runner-specific fallback (conda: ``cogpy``; pixi: default env)
-    5. Bare ``["snakemake"]``
+    3. Project config ``code.envs.default``
+    4. Pixi projects: ``pixi run snakemake`` (never fall through to PATH)
+    5. Conda projects: ``snakemake`` on PATH (with conda wrapping)
+    6. Conda fallback (``cogpy``)
+    7. Bare ``["snakemake"]``
@@ -212,18 +211,20 @@
-    # 2.5. Project config: code.envs.default
+    # 3. Project config: code.envs.default
     # This check comes before PATH search so the MCP server's own conda env
     # (e.g. 'rag') is never picked up when a project env is configured.
     default_env = _resolve_default_env_name()
     if default_env:
         return _run_cmd(default_env, "snakemake")

-    # 3. On PATH
+    # 4. Pixi proje
```

